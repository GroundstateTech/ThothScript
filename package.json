{
  "name": "thothscript",
  "productName": "ThothScript",
  "version": "0.3.1",
  "description": "A lightweight Electron desktop editor for plain text, code, Markdown, workspaces, search, recovery, printing, and PDF export.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "doctor": "node scripts/doctor.js",
    "check": "node --check main.js && node --check preload.js && node --check renderer.js && node --check community-ui.js && node --check scripts/doctor.js",
    "test": "node --test tests/*.test.js",
    "verify": "npm run doctor && npm run check && npm test",
    "package-win": "npx @electron/packager . ThothScript --platform=win32 --arch=x64 --overwrite"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "devDependencies": {
    "@electron/packager": "^20.3.0",
    "electron": "^44.1.1"
  },
  "private": true
}
